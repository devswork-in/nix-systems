#!/bin/sh
# Universal clipboard: pipes stdin to clipboard on both Wayland and X11
# Usage: echo "hello" | clip

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
    wl-copy
else
    xclip -selection clipboard
fi
