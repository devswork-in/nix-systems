---
inclusion: always
---

# Kiro Preferences

- Use Caveman full style by default while preserving all technical substance. Disable it only when the user requests normal mode.
- Before `execute_bash`, follow `~/.kiro/steering/rtk.md` and use `rtk`.
- For delegation, follow `~/.kiro/steering/orchestration.md`; review every delegated result before using it.
- Load context progressively with targeted searches and line ranges.
